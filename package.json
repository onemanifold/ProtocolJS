{
  "name": "protocol.js",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "chai": "^4.3.8",
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "node --inspect-brk --experimental-vm-modules node_modules/jest/bin/jest.js --config ./spec/.jestconfig.json"
  },
  "dependencies": {
    "chai-as-promised": "^7.1.1"
  },
  "wallaby": {
    "env": {
      "params": {
        "runner": "--experimental-vm-modules"
      },
      "files": [
        "src/**/*.js"
      ],
  
      "tests": [
        "test/**/*.spec.js"
      ]
    }
  }
}
